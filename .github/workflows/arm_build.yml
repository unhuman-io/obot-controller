name: Arm build

on: [push]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - project: boost_g474
          - project: fabulab_f446
            config: motor_enc
          - project: freebot_g474
            config: pz
    runs-on: ubuntu-latest
    steps:
    - name: install dfu-suffix
      run: sudo apt-get install -y dfu-util
    - uses: actions/checkout@master
      with:
        submodules: recursive
    - name: arm-none-eabi-gcc
      timeout-minutes: 10
      uses: fiam/arm-none-eabi-gcc@master
      with:
        release: '9-2020-q2'
    - name: make ${{ matrix.project }}${{ matrix.config && ' '}}${{ matrix.config }}
      run: make -C ${{ matrix.project }}${{ matrix.config && ' CONFIG='}}${{ matrix.config }} -j2
    - name: Archive ${{ matrix.project }}${{ matrix.config && ' '}}${{ matrix.config }}
      uses: actions/upload-artifact@v2
      with:
        name: ${{ matrix.project }}${{ matrix.config && ' '}}${{ matrix.config }}
        path: |
          ${{ matrix.project }}/build/${{ matrix.project }}${{ matrix.config && ' '}}${{ matrix.config }}.elf
          ${{ matrix.project }}/build/${{ matrix.project }}${{ matrix.config && ' '}}${{ matrix.config }}.bin